name: "loan_intelligence"
version: "1.0.0"
config-version: 2

profile: "loan_intelligence"

model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  loan_intelligence:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
